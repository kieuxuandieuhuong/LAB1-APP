<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- View background color -->
    <solid android:color="@android:color/white" /> <!-- Adjust this color as needed -->
    <!-- View border color and width -->
    <stroke
        android:width="2dp"
        android:color="@color/button_color_secondary" /> <!-- Define this color in your colors.xml -->
    <solid android:color="@color/material_dynamic_neutral_variant95"/>
    <!-- Optional: Rounded corners -->
    <corners android:radius="4dp" />
</shape>
